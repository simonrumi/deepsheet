{
   "name": "parent",
   "version": "1.0.0",
   "description": "The parent, i.e. root folder for Deepsheet",
   "author": "Simon Amarasingham",
   "repository": "https://github.com/simonrumi/deepsheet",
   "license": "ISC",
   "main": "index.js",
   "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "tailwind:css": "tailwind build client/src/css/tailwind.src.css -c client/tailwind.config.js -o client/src/css/tailwind.css",
      "server": "nodemon ./server/index.js",
      "client": "npm run start --prefix client",
      "dev": "npm run tailwind:css && concurrently \"npm run server\" \"npm run client\" \"npm run lambda\"",
      "postinstall": "cd server && npm install && cd ../client && npm install",
      "prebuild": "npm run tailwind:css",
      "build": "cd client && npm run build && cd ../server && npm run build:lambda",
      "lambda": "npm run lambda --prefix server"
   },
   "dependencies": {
      "tailwindcss": "^1.2.0"
   },
   "devDependencies": {
      "concurrently": "^4.1.2",
      "nodemon": "^1.19.4"
   }
}
