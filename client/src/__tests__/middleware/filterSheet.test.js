// import filterSheet from '../middleware/filterSheet';

// TODO need to call like this: filterSheet(store, next, action)
// so need to mock all those things
// note next is a function so there could just be something we can use to see if it has been called


describe('helpers', () => {
    it('hello world test', () => {
       expect(true).toBe(true);
    })
 });