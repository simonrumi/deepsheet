module.exports = {
   plugins: {
      tailwindcss: {},
      autoprefixer: {},
   },
};

// note this is probably not actually used, but doesn't seem necessary
// if it becomes necessary ready this on using CRACO which allows for configuration of create-react-app as well
// https://blog.logrocket.com/tailwind-css-configure-create-react-app/